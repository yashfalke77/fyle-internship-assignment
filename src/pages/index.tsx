import UserDetails from "components/userDetails/UserDetails";
import React from "react";

const Home: React.FC = () => {
  return (
    <div>
      < UserDetails />
    </div>
  );
};

export default Home;
